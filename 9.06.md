# 📓 毕业设计开发日志 (2026-09-06)

## **🌟 今日核心成果**
> **算法大脑正式诞生**：完成了从原始财务数据到 LightGBM 预警模型的全流程开发。模型在测试集上表现卓越，**AUC 达到 0.8686**，对风险企业（ST）的**召回率（Recall）高达 72%**。

---

## **🛠 技术开发细节**

### **1. 多维数据融合 (Data Integration)**
- **操作**：通过 `Stkcd` 和 `Accper` 唯一键，将偿债能力、经营能力、发展能力三个维度的 Excel 表格进行了横向合并。
- **成果**：构建了包含 23 个核心财务指标的原始特征库，样本总量达 **50,549** 条。
- **脚本**：`src/merge_features.py`

### **2. ST 标签自动化对齐 (Automated Labeling)**
- **操作**：解析 RESSET 数据库的特别处理（ST）记录，解决了数字编码（1-15）与复杂时序（实施 vs 撤销）的对齐难题。
- **成果**：成功识别出 **1,426** 条真实风险样本，占比 2.82%，为模型提供了可靠的训练目标。
- **脚本**：`src/label_data.py`

### **3. 特征工程与样本均衡 (Feature Engineering)**
- **数据清洗**：实现了全局中位数填充（SimpleImputer），解决了 SMOTE 算法对 NaN 敏感的问题。
- **特征筛选**：利用 `VarianceThreshold` 剔除低贡献指标，保留了 17 个最具区分度的特征。
- **样本均衡**：针对 2.8% 的极低风险率，引入 **SMOTE 算法**将训练集 ST 样本从 1,168 条扩充至 **38,185** 条，实现了 1:1 的完美平衡。
- **脚本**：`src/feature_engineering.py`

### **4. LightGBM 建模与评估 (Modeling & Evaluation)**
- **模型配置**：采用 GBDT 提升树，设置 `early_stopping` 防止过拟合，学习率 0.05。
- **性能指标**：
    - **AUC**: 0.8686
    - **Recall (Label 1)**: 0.72 (能够提前捕捉 72% 的财务危机)
    - **核心特征**：前三名分别为 F080601A（发展能力）、F010701B（偿债能力）、F010601A（偿债能力）。
- **脚本**：`src/train_model.py`
- **产出**：`lgb_model.txt` (最终模型文件)

### **5. B/S 系统连通性 (System Connectivity)**
- **架构**：Vue 3 + SpringBoot + Docker MySQL。
- **成果**：实现了前后端跨域通信，前端 Dashboard 能够实时调用后端接口，展示数据库中的真实样本统计量。

---

## **📅 下步工作计划**
1. **模型接口化**：在后端实现 Python 脚本的异步调用，将训练好的模型集成到 Web 预测页面。
2. **可视化增强**：在前端展示模型预测的概率分布及特征贡献度排名。
3. **系统完善**：开发用户登录鉴权与历史预测记录管理功能。

---
**签字：** AI 助教
**状态：** 核心算法阶段已圆满收官！
