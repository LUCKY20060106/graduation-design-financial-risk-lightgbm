# 📓 毕业设计开发日志 (2026-08-28)

## **1. 今日目标**
*   [x] 验证并初步清洗偿债能力财务指标数据。
*   [x] 搭建 B/S 系统基础骨架（SpringBoot + Vue 3）。
*   [x] 实现前后端全链路打通，连接数据库。

---

## **2. 数据处理环节 (Python)**
*   **创建处理脚本**：编写了 `src/data_preprocessing.py`。
*   **环境补丁**：安装了 `openpyxl` 库以支持 pandas 读取 `.xlsx` 文件。
*   **执行清洗逻辑**：
    *   读取 `偿债能力215502592/FI_T1.xlsx` (318,148 条)。
    *   过滤非合并报表及非年报数据，最终保留 **50,549 条** 核心年报样本。
    *   对 `F010701B` (利息保障倍数) 等指标进行了 **中位数填充**。
*   **数据持久化**：生成了 `solvency_features.pkl` 二进制文件。
*   **统计提取**：编写 `src/extract_stats.py` 提取了 10 年间的平均资产负债率趋势数据。

---

## **3. 后端开发环节 (SpringBoot)**
*   **项目初始化**：创建了 `backend` 文件夹，配置 `pom.xml`（集成 Web, JPA, MySQL, Lombok）。
*   **接口实现**：
    *   创建 `TestController`，提供 `/api/hello` 和 `/api/stats` 接口。
    *   配置 `CorsConfig` 实现 **跨域支持** (允许前端 5173 访问)。
*   **数据库持久化**：
    *   定义实体类 `User` 和 `CompanyInfo`。
    *   创建 Repository 接口，实现数据库的 CRUD 能力。
*   **配置修复**：解决了 MySQL 8.0 `Public Key Retrieval is not allowed` 的连接安全报错。

---

## **4. 前端开发环节 (Vue 3)**
*   **项目初始化**：使用 Vite 初始化 Vue 3 项目，安装 `element-plus`、`@element-plus/icons-vue`、`axios`、`echarts` 和 `vue-router`。
*   **界面布局**：
    *   重构 `App.vue`，实现侧边栏导航和面包屑联动。
    *   创建 `views/Dashboard.vue`（仪表盘）和 `views/DataImport.vue`（数据导入）。
*   **功能实现**：
    *   **可视化图表**：首页成功渲染 ECharts 混合图，动态展示 2015-2024 财务指标趋势。
    *   **交互模拟**：实现“数据导入”页面的文件上传 UI 及模拟解析日志显示。
    *   **通信测试**：实现右上角“测试后端连接”按钮，实时展示后端及数据库状态。

---

## **5. 基础设施环节 (Docker & SQL)**
*   **容器化部署**：
    *   使用 Docker 部署 MySQL 8.0。
    *   解决端口占用冲突（宿主机端口映射改为 **3307**）。
*   **数据库初始化**：
    *   编写并执行 `sql/schema.sql`。
    *   创建库 `risk_db` 及四张核心表：用户表、企业表、指标表、预测结果表。

---

## **6. 当前系统状态**
*   **前端**：`http://localhost:5173/` (正常运行)
*   **后端**：`http://localhost:8080/` (已连接 MySQL 3307)
*   **测试结果**：`后端服务已成功启动！ | 50549条数据已就绪，当前系统用户数：1`

---

## **7. 待办事项 (Next Steps)**
1.  [ ] 待获取 CSMAR "ST记录表" 后进行标签匹配。
2.  [ ] 开发真正的 Python 预测 API (Flask/FastAPI)。
3.  [ ] 实现前端用户登录与 Session 保持。
