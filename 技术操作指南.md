本指南将详细列出您计算机毕业设计项目中所需的技术操作步骤，涵盖环境搭建、模型开发、实验分析和系统实现。

### **1. 环境搭建与依赖安装**

*   **1.1 Python环境**
    *   **目标**：搭建Python环境，安装机器学习相关库。
    *   **库列表**：`lightgbm`, `scikit-learn`, `pandas`, `numpy`, `shap`, `imblearn`。
    *   **状态**：待办
*   **1.2 Java环境**
    *   **目标**：搭建Java环境，配置Maven或Gradle。
    *   **关键组件**：JDK (推荐LTS版本，如JDK 17+), Maven 或 Gradle。
    *   **步骤**：
        1.  安装JDK并配置 `JAVA_HOME` 环境变量。
        2.  安装Maven或Gradle并配置相应的环境变量（如 `MAVEN_HOME`）。
        3.  验证安装：`java -version`, `javac -version`, `mvn -version` 或 `gradle -v`。
    *   **状态**：**已完成**
*   **1.3 Node.js环境**
    *   **目标**：搭建Node.js环境，安装npm/yarn，用于Vue前端开发。
    *   **关键组件**：Node.js (包含npm), yarn (可选)。
    *   **步骤**：
        1.  下载并安装Node.js。
        2.  验证安装：`node -v`, `npm -v`。
        3.  （可选）安装yarn：`npm install -g yarn`。
    *   **状态**：**已完成**

### **2. 数据准备**

*   **2.1 收集并初步清洗上市公司财务数据**
    *   **目标**：获取项目所需的数据集，并进行初步的质量检查。
    *   **内容**：数据来源、数据结构、标签定义（ST/非ST）、训练集/测试集划分（建议按时间划分）。
    *   **状态**：待办

### **3. LightGBM模型开发**

*   **3.1 实现LightGBM模型的数据加载与预处理**
    *   **目标**：将原始数据转换为模型可用的格式。
    *   **内容**：处理缺失值、异常值、标准化（说明哪些算法需要，哪些不需要）、类别编码。
    *   **状态**：待办
*   **3.2 实现特征选择（Variance Threshold）**
    *   **目标**：删除低方差特征，减少噪声，提高训练速度。
    *   **状态**：待办
*   **3.3 实现类别不平衡处理（Adaptive SMOTE）**
    *   **目标**：解决ST样本少、正常公司多的类别不平衡问题，提高模型对少数类的识别能力。
    *   **内容**：Adaptive SMOTE的实现细节和理由。
    *   **状态**：待办
*   **3.4 实现LightGBM模型训练、参数优化和Early Stopping**
    *   **目标**：训练优化后的LightGBM模型。
    *   **内容**：Class Weight的应用、参数优化（交叉验证）、Early Stopping。
    *   **状态**：待办

### **4. 模型评估与分析**

*   **4.1 实现模型评估与分析（性能指标、图表）**
    *   **目标**：全面评估模型性能。
    *   **指标**：Accuracy, Precision, Recall, F1, AUC。
    *   **图表**：ROC曲线、混淆矩阵、PR曲线、柱状图、折线图。
    *   **状态**：待办
*   **4.2 实现SHAP模型解释**
    *   **目标**：增强模型可解释性，理解特征对预测结果的影响。
    *   **内容**：Shapley Value的应用，生成SHAP图。
    *   **状态**：待办

### **5. B/S系统实现**

*   **5.1 搭建SpringBoot后端API**
    *   **目标**：构建后端服务，提供数据接口。
    *   **内容**：RESTful API设计，与数据库交互，调用Python模型服务。
    *   **状态**：待办
*   **5.2 封装Python模型为API服务**
    *   **目标**：将训练好的LightGBM模型封装为独立的微服务。
    *   **内容**：使用Flask/FastAPI等框架，提供预测接口。
    *   **状态**：待办
*   **5.3 开发Vue前端界面**
    *   **目标**：实现用户交互界面。
    *   **模块**：登录、企业管理、Excel导入、模型预测、风险分析、历史查询、数据导出、Dashboard、图表展示。
    *   **状态**：待办
*   **5.4 设计并实现MySQL数据库**
    *   **目标**：存储项目相关数据。
    *   **表结构**：用户表、企业信息表、财务指标表、预测结果表（可选：预测日志表）。
    *   **状态**：待办
